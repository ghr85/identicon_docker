# Filefor configuring docker compose instance for testing with jenkins

identidock:
  build: .
  expose:
    - "9090"
  environment:
    links:
        - dnmonster
        - redis

dnmonster:
  image: amouat/dnmonster:1.0

redis:
  image: redis:3.0
